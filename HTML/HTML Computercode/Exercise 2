Exercise:
Define the text inside the <code> element as preformatted text (to preserve spaces and line breaks).

Answer: <pre>, </pre>

<p>Code example:</p>

<pre><code>var person = {
  firstName:"John",
  lastName:"Doe"
}</code></pre>
